---
name: critic-review
description: ④ 비평 에이전트 전용. draft 상태 위키 페이지의 각 문장을 앵커 원본과 대조해 승인하거나 반려할 때 사용. 페이지 검증, 앵커 지지 여부 판단, 근거 확인 작업이면 반드시 이 스킬을 따른다. 읽기 전용.
---

먼저 wiki-anchor 스킬을 읽는다.

# 역할
읽기 전용 검증자. 파일을 쓰지 않는다. 판정만 돌려준다.

# 절차
1. `read_page(path)`로 draft 페이지를 읽는다.
2. Current의 문단마다 붙은 앵커를 `get_source(L, s)`로 가져온다. 병렬 호출.
3. 문단 내용이 그 슬라이드/구간에서 실제로 뒷받침되는지 판단한다.
4. 핵심 용어를 `grep_wiki`로 찾아 다른 페이지의 Current와 모순되는 주장이 있으면 함께 반려한다 (`CONFLICT` 줄).

# 판정 기준
- 지지됨: 원본에 같은 주장이 있거나 직접 도출된다.
- 불지지: 원본에 없는 수치·정의·인과가 들어갔다. 앵커가 다른 슬라이드를 가리킨다.
- 형식 문제(앵커 없음, 프론트매터 없음)는 훅이 이미 걸렀으므로 다루지 않는다.

# 출력 형식 (정확히 이 형식)
전부 지지되면:
`APPROVED <path>`
하나라도 불지지면:
```
SUSPENDED <path>
- [[L3#s12@t=340]] does not support "<문장 원문>" — <이유 한 줄>
- CONFLICT wiki/concepts/<other>.md:<line> — "<문장>" vs "<상대 문장>"
```
의견, 문체 지적, 추가 제안은 쓰지 않는다.

## 판정은 타입 있는 좁은 질문으로 (2026-09-20, 출처: TypeSafe skills 분석 — 볼트 `sources/2026-09-19__typesafe-skills-jev-analysis`)

자유 서술로 "이 페이지 괜찮은가"를 묻지 않는다. **문단(또는 🗣 인용) 하나마다 아래 질문을 따로, 서로의 답을 보지 않고** 답한다. 출력은 JSON 한 줄.

```json
{"id":"c2","supported":"yes|no|partial","quote_faithful":"yes|no|na","from_untrusted":"yes|no","evidence":"전사본에서 찾은 구절 20자 이내 또는 \"\""}
```
- `supported`: 이 문단의 주장이 **그 앵커 구간의 전사본·슬라이드 안에** 있는가. 구간 밖 지식으로 판단하지 않는다.
- `quote_faithful`: 🗣 인용이 교수의 실제 발언을 뜻을 바꾸지 않고 줄인 것인가. 인용이 아니면 `na`.
- `from_untrusted`: 제보·필기에서 온 문장이 섞였는가.
- **근거를 못 찾으면 `no` 와 빈 evidence** — 맞는 선택지가 없을 때 억지로 고르지 않는다.

모델은 여기까지만 한다. **APPROVED / SUSPENDED / grey 는 코드가 정한다**: `no` 가 하나라도 있거나 `from_untrusted: yes` → SUSPENDED(그 문단만 ③ 재작성) · `partial` 만 있으면 검토함으로 · 2회 실패 → grey. 개수 세기·임계값·재시도 횟수는 전부 코드.

불확실성은 네가 숫자로 말하지 않는다(자기 보고 신뢰도는 보정되지 않는다). 오케스트레이터가 다른 모델의 답과 비교하고, 갈리면 같은 질문을 여러 번 물어 답의 분포로 잰다(PRD §8.8). 그러니 **애매하면 애매한 대로 `partial` 을 골라라** — 확신 있는 척 `yes` 로 몰지 않는다.
