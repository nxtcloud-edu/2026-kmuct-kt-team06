# 발표 숫자 (실측) — L1 컴퓨터구조 week_2_2 "RISC-V ISA"

> 전부 실측값이다(추정 없음). 출처 명령을 각 절에 적었다.
> 측정 대상: 강의 L1 전체를 `pipeline.run` 으로 ①적재~④비평까지 1회 완주(run `28695ee2`).
> 마지막 주제 s21-21 은 라이브 시연용으로 남겼다 → 실제 컴파일은 7/8 주제.
> 재현: `LLM_PROVIDER=openai python3 -m pipeline.run L1 --title "RISC-V ISA" --course "컴퓨터구조" --hold-last`

## 1. 쓰기 훅 (WritePolicy) — `python3 tools/hook_metrics.py`

| 숫자 | 값 | 발표에서 하는 말 |
|---|---|---|
| 총 쓰기 시도 | **20** | 모델(+정렬 코드)이 위키에 쓰려 한 횟수 |
| 통과 / 거부 | **17 / 3** | 훅이 15%를 막았다 |
| **거부 후 재작성 통과 (rescued)** | **3** | **에이전트가 거부 사유를 읽고 스스로 고쳐 3건 전부 통과** ← 심사 포인트② |
| 한 번에 통과 (first_pass) | 14 | |
| 평균 시도 횟수 | 1.18 | 프롬프트가 좋아지면 1.0 에 가까워진다 |
| 규칙별 거부 | **R03 프론트매터 누락 ×3** | 모델이 가장 자주 틀린 곳 |
| 에이전트별 | compile: allow 16 / deny 3 · align: allow 1 | 쓰기 루트 분리가 실제로 작동 |
| 위키에 들어간 앵커 | **262** | 262개 문장이 "몇 강 몇 초"로 원본을 가리킨다 |
| 🗣 교수님 인용 | **32** | |

거부 3건은 전부 R03(프론트매터 누락)이었고, **3건 모두 재작성으로 통과**했다(rescued=denied=3). 끝내 못 쓴 시도(blocked_for_good)는 0.

## 2. 인용 대조 (🗣 ↔ 전사본, 코드) — `pipeline.critic.quote_check`

**🗣 인용 23개 중 23개 통과 (23/23).**
각 🗣 인용문을 그 앵커 구간의 전사본과 문자 유사도(difflib)로 대조 — 문턱 0.5 미만이면 "인용 왜곡"으로 검토함에 올린다. 이번 산출물은 모든 인용이 실제 발언에서 왔다.

| 페이지 | 통과/전체 |
|---|---|
| L1_ISA_개요와_RISC | 4/4 |
| L1_레지스터_파일과_스택_개요 | 2/2 |
| L1_메모리_오퍼랜드와_엔디안 | 3/3 |
| L1_산술_연산과_설계_원칙 | 3/3 |
| L1_즉시수와 정수 표현 | 9/9 |
| L1_프로세스_메모리_레이아웃과_데이터_전송_도입 | 2/2 |

## 3. 시험 범위 커버리지 — `python3 tools/coverage.py graph`

**커버리지 9/10** (자료구조 그래프 단원 시험 제보 기준, L3 견본 대응).
시험 제보 10항목을 위키에서 grep → 9개는 강의에 있음, **"위상정렬"만 강의 근거 없음**(정직하게 미커버로 표시).
> 시험 제보(signals)는 힌트지 정본이 아니다 — 앵커로 인용하지 않고 페이지 유무 판단에만 쓴다.

## 4. 컴파일 산출물

- **7개 주제 컴파일, allow 페이지 16개** (강의 노트 6 + 개념 페이지 10)
- 앵커가 실제 프레임/전사 구간을 가리킴(훅 FrameGuard·WritePolicy 가 검증)
- 마지막 주제 s21-21 은 라이브 시연용으로 보류 → 발표 때 `python3 -m pipeline.run L1 --topic s21-21 --live` 로 30초 안에 deny→allow 를 화면에 띄운다

## 5. 문제 정의 (설문 N=96, `docs/PRD.md`)

- 요약 AI 를 쓴 학생 75명 중 **85%가 요약을 읽고도 원본으로 되돌아갔다**
  - 이유: 시험에 나올 표현 확인 59% · AI 요약이 틀린 것 같아서 52%
- → 그래서 **모든 문장에 "몇 강 몇 초" 앵커**를 붙여 한 번에 원본으로 가게 했다(위 262개 앵커).
